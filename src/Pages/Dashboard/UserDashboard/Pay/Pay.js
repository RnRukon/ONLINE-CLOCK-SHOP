import { Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <div>
            <Typography color="secondary" className="text-center" variant="h3">
                Payment system coming soon
            </Typography>
        </div>
    );
};

export default Pay;